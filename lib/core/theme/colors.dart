import 'package:flutter/material.dart';

class AppColors {
  static const bgStart = Color(0xFF0E0F1A);
  static const bgMid = Color(0xFF171A28);
  static const bgEnd = Color(0xFF212638);
  static const glassFill = Color(
    0x66FFFFFF,
  ); // 40% white
  static const glassBorder = Color(
    0x33FFFFFF,
  ); // 20% white
  static const accent = Color(0xFF5EF8C1);
  static const accentGlow = Color(0xFF86FFD7);
}
